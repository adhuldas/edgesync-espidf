CONFIG_PARTITION_TABLE_CUSTOM=y
CONFIG_PARTITION_TABLE_CUSTOM_FILENAME="partitions.csv"

# EdgeSync publishes over HTTPS; make sure the mbedTLS certificate bundle
# used by esp_crt_bundle_attach() is enabled (this is the IDF default).
CONFIG_MBEDTLS_CERTIFICATE_BUNDLE=y
